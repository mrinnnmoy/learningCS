# Security Checklist — Week 31

| #   | Question (Concept 11)                                                                                                | Applies to                                                                                    | Status / Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| --- | -------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | CEI followed on every state-changing external call? Reentrancy guard where CEI alone feels fragile?                  | FlashLoanProvider, SimplePool                                                                 | **Reviewed / accepted for this lab.** Slither reports `reentrancy-benign` for `SimplePool`. The contracts use simple ERC-20 interactions and are controlled teaching examples; no exploitable reentrancy is demonstrated by the tests. `FlashLoanProvider` transfers the loan before the receiver callback by design, then verifies repayment.                                                                                                                                                                                                        |
| 2   | Every `unchecked` block commented with a proof overflow can't happen?                                                | (none used this week — note that explicitly, don't leave blank)                               | **PASS.** No `unchecked` arithmetic blocks are used this week. Solidity 0.8.36 checked arithmetic remains enabled.                                                                                                                                                                                                                                                                                                                                                                                                                                    |
| 3   | Every privileged function checks `msg.sender`, never `tx.origin`? Every initializer constructor-only or guarded?     | VulnerableVault (FAILS — Easy's own point) vs. FixedVault                                     | **Reviewed.** No `tx.origin` authentication or initializer pattern is used in the security-hard contracts. `TrustedOracle` uses an owner-based authorization model.                                                                                                                                                                                                                                                                                                                                                                                   |
| 4   | Anything relying on `block.timestamp` where predictability matters?                                                  | VulnerableLottery (FAILS) vs. n/a for Medium's other contracts                                | **INTENTIONAL FAILURE / VULNERABILITY.** `VulnerableLottery.drawWinner()` derives the winner from `block.timestamp`. `LotterySecurity.t.sol` demonstrates that a chosen timestamp can be used to predict the winner before `drawWinner()` executes. This is intentional for the timestamp-randomness security exercise.                                                                                                                                                                                                                               |
| 5   | Any price-dependent logic reading a single-transaction-manipulable source instead of a TWAP/external oracle?         | VulnerableLendingPool (FAILS) vs. GuardedLendingPool                                          | **INTENTIONAL FAILURE / VULNERABILITY.** `VulnerableLendingPool` reads the AMM spot price from `SimplePool`, which can be manipulated within the same transaction using a flash loan. The exploit test successfully inflates the collateral valuation and produces approximately 0.201 tokenB profit. `GuardedLendingPool` uses `TrustedOracle` instead, and the identical attack reverts.                                                                                                                                                            |
| 6   | Any unbounded loop, or funds pushed rather than pulled?                                                              | VulnerableAuction (FAILS) vs. FixedAuction                                                    | **Reviewed.** The `VulnerableAuction` / `FixedAuction` contracts from the template are not part of this security-hard implementation. No unbounded loop is used in the implemented attack path. The lottery intentionally uses a direct push payment to the selected winner as part of the exercise.                                                                                                                                                                                                                                                  |
| 7   | Every `delegatecall` target a compile-time constant, or as tightly access-controlled as the most sensitive function? | (none used this week — note that explicitly)                                                  | **PASS.** No `delegatecall` is used in the security-hard contracts.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 8   | Has Slither actually been run, and every finding fixed or explicitly accepted with a reason?                         | Whole security-hard/ project — MockERC20's open mint() explicitly accepted, test-fixture-only | **REVIEWED / ACCEPTED.** `slither .` completed successfully and reported 24 findings across 9 contracts: unchecked ERC-20 transfer/transferFrom return values, unused return values, a missing zero-address check in `TrustedOracle`, and `reentrancy-benign` findings. These are explicitly accepted/documented for this controlled teaching project. `MockERC20`'s open `mint()` is also a test-fixture-only design and is intentionally accepted. Production code should check ERC-20 return values and validate privileged constructor arguments. |
